package frc.robot.testing;

//Offset
public class Offset {
    
}if(_gamepad.getRightBumperPressed()){if(this.armLiftOffset==Constants.armLiftOffset){this.armLiftOffset=0;}else{this.armLiftOffset=Constants.armLiftOffset;}}arm.liftPeriodic(lift,extend,open);